//this could be in data table in db
//object, keys=>role names, code to identify the role
const ROLES_LIST = {
    "Admin": 5150,
    "Editor": 1984,
    "User": 2001 //registered users
}

module.exports = ROLES_LIST;